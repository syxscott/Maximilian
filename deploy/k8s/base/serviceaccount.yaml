# ServiceAccount for the API and worker pods. No special RBAC bindings
# are required for in-cluster communication — pods talk to postgres / redis
# via the service DNS names, which work without explicit permissions.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: maximilian
  namespace: maximilian