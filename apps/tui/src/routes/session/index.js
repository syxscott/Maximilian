/**
 * Session route re-export.
 *
 * The canonical `Session` component lives in `./session.tsx`; this module
 * mirrors OpenCode's `routes/session/index.tsx` for ergonomic imports.
 */
export { Session } from "./session";
